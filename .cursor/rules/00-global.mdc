---
description: kol-mail-desk-v2 前端全局规则（每次会话强制加载）
alwaysApply: true
---

# 全局规则

## 跨会话续接协议（必读 / 必更）

**每次新会话开始，第一件事**：

1. `Read /Users/chenkaifeng/code/kol-mail-desk-v2-docs/specs/STATUS.md`
   - 确认当前 Phase 和活跃 ticket
   - 确认阻塞项
2. `Read /Users/chenkaifeng/code/kol-mail-desk-v2-docs/specs/BACKLOG.md` 的当前活跃 ticket 详细条目
3. 然后再开始执行任务

**每次完成一个 ticket、解决阻塞、进入新 Phase**，必须同步更新三处：

1. `kol-mail-desk-v2-docs/specs/STATUS.md`
2. `kol-mail-desk-v2-docs/specs/BACKLOG.md`（ticket 行 `⬜ → 🔄 → ✅`）
3. `kol-mail-desk-v2-docs/specs/05-feature-parity.md`（如该 ticket 关联 feature）

> 漏更新是流程缺陷，PR Review 不通过。

## 重构上下文

- 这是 kol-mail-desk 全量重构的 v2 前端
- 旧仓库 `/Users/chenkaifeng/code/kol-mail-desk` **只读参考**，禁止修改；可以拷贝可复用组件
- 设计规格在 `/Users/chenkaifeng/code/kol-mail-desk-v2-docs/specs/`
- 前端复用评估：`specs/03-frontend-reuse.md`
- 本地启动：`specs/SETUP.md`

## 工作模式

- 推荐用 `~/code/maildesk-v2.code-workspace`（multi-root workspace）打开项目，能同时看到 docs / backend / web
- 单次会话尽量完成一个垂直切片：**OpenAPI 契约 → 后端实现 → 前端调用 → E2E**
- 前端的 TS 类型从 `kol-mail-desk-v2-docs/specs/api-contract-v1.yaml` 由 `pnpm gen:api` 自动生成；改类型 = 改契约，不要手动改生成产物
- 后端字段改名 / 改语义 = 破坏性变更，必须等后端先改契约 + 前端 `pnpm gen:api` 后再动 UI

## 语言约定

- **所有用户可见字符串**：简体中文
- 代码标识符：英文
- 文件名：英文 kebab-case 或 camelCase

## 接口约定

- 前端不再直连 Supabase / Gmail / 飞书 / Kimi
- 所有数据通过 `lib/api-client/`（由 OpenAPI 自动生成的 TS 类型）调后端
- API 契约源：`/Users/chenkaifeng/code/kol-mail-desk-v2-docs/specs/api-contract-v1.yaml`

## Phase 0 当前阶段约束

- 仅做骨架 + 复用组件迁入计划，不实现业务逻辑
- 进入 Phase 1 前必须满足 `04-phases.md § Phase 1 进入准入`

## Cursor 操作硬约束

- 不在 `node_modules/` / `.next/` / `out/` 编辑
- 不读写 `.env.local` 等含密钥文件
- 不主动 git push（除非用户明确请求）
- 不擅自跨 ticket 扩大改动范围
